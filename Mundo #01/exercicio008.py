# Programa que converta medida de metros para centimetros e milimetros

num = float(input("Digite a medida em metro : "))
print("{}m é o mesmo que {:.0f}cm e {:.0f}mm".format(num,num*100,num*1000))

